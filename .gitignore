__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv/
venv/
.mypy_cache/
.ruff_cache/
.mycoder_history
.DS_Store
.pytest_cache/
.tmp/
# Phase 5 runtime state: project-level memory db + plan store live here
.mycoder/
# eval_bench runtime artifacts
results/
eval_bench/workspace/
workspaces/
# Personal interview notes and local reports are intentionally not published.
# The three documentation files already tracked under docs/ remain tracked;
# this rule prevents new private material from being added accidentally.
docs/
# Local agent working memory — personal notes, same policy as docs/ above.
.workbuddy/
# Runtime logs: the official SWE-bench harness writes logs/run_evaluation/ here.
logs/
# Harness reports are written to the CWD as <run>.mycoder-<hash>.json. Pinned to
# the repository root so a same-named file under a package is never hidden.
/*.mycoder-*.json
